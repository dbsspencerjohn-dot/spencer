version: "3.9"
services:
  flask-app:
    build: .
    container_name: vulnerability_issues_tracker
    ports:
      - "5000:5000"
    volumes:
      - ./database.db:/app/database.db
    restart: unless-stopped
